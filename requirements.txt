pandas>=2.0
numpy>=1.24
exchange_calendars>=4.5

# Streamlit UI (app.py). Not needed for the headless runner, live.py.
streamlit>=1.37
plotly>=5.18

yfinance>=0.2          # optional: only for the Yahoo Finance feed
ib_async>=1.0          # optional: only for the Interactive Brokers feed
